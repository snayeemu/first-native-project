const {StyleSheet} = require('react-native');

export default exStyles = StyleSheet.create({
  commonTextStyles: {
    fontSize: 35,
    color: 'white',
    backgroundColor: 'red',
    marginTop: 20,
  },
});
